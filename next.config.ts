import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // pageExtensions: ["page.tsx"], //only allow page files with file extensions `jsx` or `tsx` need to setting as ['page.tsx', 'page.ts', 'page.jsx', 'page.js']
  /* config options here */
};

export default nextConfig;
